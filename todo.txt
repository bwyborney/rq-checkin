Make functions for generating check-in items based on fed formats
    event listener for each button, passing its key which is also its id
    for percent/amps, do onChange and pass its value

make the event action for each of those buttons

Add a button for converting the page

Add a button for printing the page

Make a starting variable which would get passed to this including default values

Test

Integrate with Chrome extension
    how to get page to pop up
    how to pass variable data to it
    how to output the variable 

Integrate with custom form for saving data

Add an override button for certain situations
    Add a way to track whether this has been done or not

Add a backup in case someone fills this out before fillin out the customer, device data, if they choose anonymous customer, etc.